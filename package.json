{
  "name": "oh-my-agents",
  "version": "0.0.0",
  "private": true,
  "description": "Serve a machine's coding agents over your own tailnet, with one command.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "bin": {
    "oh-my-agents": "dist/src/cli/main.js"
  },
  "scripts": {
    "build": "tsc --build --force && node scripts/copy-web.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test 'dist/test/*.test.js'"
  },
  "devDependencies": {
    "@types/node": "20.19.9",
    "typescript": "5.6.3"
  }
}
